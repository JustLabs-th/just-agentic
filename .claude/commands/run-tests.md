Run the full test suite and summarize results.

```bash
cd /Users/jin/dev/just-agentic && python3 -m pytest tests/ -v 2>&1
```

Report: total passed/failed, any failing test names, and root cause if obvious.
